import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
    borderRadius: "2px",

    colors: {
        primary: "#1D32FA",
        secondary: "white",
        light: "#EEEFF3"
    }
};

export { theme };
